class AES():
    @staticmethod
    def dblock_strings(s, k):
        """Return a string that is the plaintext decryption of base64-encoded
           string s under plaintext key k, s and k each representing a 128 bit
           number.
        """
        return ""
